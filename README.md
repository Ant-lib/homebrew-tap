# Ant-lib Homebrew tap

```sh
brew install --cask --no-quarantine ant-lib/tap/hingewave
```

Casks for Ant-lib projects. Hingewave is ad-hoc signed and not notarized, hence `--no-quarantine`; without it, allow the app once under System Settings, Privacy and Security after the first launch.
